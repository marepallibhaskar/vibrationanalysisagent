# vibrationanalysisagent
Autonomy + Interactivity Autonomy: The orchestrator reads the YAML definitions (agents, tools, tasks, pipeline) and executes them without you having to manually call each step. It can infer missing inputs (like RPM from FFT), retry failed tasks, and escalate low‑confidence detections.  
